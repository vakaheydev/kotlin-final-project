# Windows Docker Desktop compatibility settings
# Testcontainers 1.21.4+ automatically detects the correct connection strategy

# Enable reuse of containers between test runs (optional)
testcontainers.reuse.enable=false

# Ryuk container cleanup - keep enabled for proper cleanup
# If experiencing issues, can be disabled with: testcontainers.ryuk.disabled=true
